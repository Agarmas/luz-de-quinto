---
import logo from '../assets/logo.webp';
import styles from './Header.module.css';

const baseUrl = import.meta.env.BASE_URL;
---
<header class={styles.siteHeader}>
	<a class={styles.brand} href={baseUrl}>
		<img src={logo.src} alt="Luz de Quinto" width="46" height="46" />
		<span>Luz de Quinto</span>
	</a>
	<nav class={styles.siteNav} aria-label="Navegación principal">
		<a href={baseUrl}>Inicio</a>
		<a href={`${baseUrl}formacion`}>Formación</a>
		<a href={`${baseUrl}sacramentos`}>Sacramentos</a>
		<a href={`${baseUrl}contacto`}>Contacto</a>
	</nav>
</header>
